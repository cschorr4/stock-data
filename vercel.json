{
  "version": 2,
  "buildCommand": "next build",
  "installCommand": "pip install -r requirements.txt && npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}